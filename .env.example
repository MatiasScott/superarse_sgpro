# .env.local - Configuración Local (NO COMMITEAR ESTE ARCHIVO)
# Copia este archivo a .env.local y ajusta los valores según tu ambiente local

# Database configuration for local development
DB_HOST_LOCAL=localhost
DB_USER_LOCAL=root
DB_PASS_LOCAL=
DB_NAME_LOCAL=superar1_landing_sgpro

# Database configuration for production
DB_HOST_PROD=localhost
DB_USER_PROD=superar1_Tics
DB_PASS_PROD=/Msvs5297*
DB_NAME_PROD=superar1_landing_sgpro
